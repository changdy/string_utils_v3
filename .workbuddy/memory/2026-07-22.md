# 2026-07-22

## 全项目代码/架构审查（StrToolkit Avalonia 版）

通读了全部 30 个 .cs 源文件、csproj/sln、prepare-web-assets.mjs、Inno 脚本、CI workflow 与三份文档，并验证 `dotnet build` 0 警告 0 错误。

**总体结论**：架构合理、工程质量高（MVVM 分层清晰、ISolver 契约简洁、端口探测/CORS/Wayland 降级等细节完善、文档与实现一致）。

**发现的问题（按优先级）**：
- P0：`MainWindow.axaml.cs` 的 `Log()` 遗留调试代码 —— 硬编码写 `c:\project\string_utils_v2\clip.log`，且记录剪贴板前 50 字符（隐私泄漏）。应删除或改 AppLog。
- P1：无测试项目（8 个 solver 是纯函数，最该有单测）；TextBox `IsReadOnly=True` 与 Electron 可编辑行为不一致且 DIFFERENCES.md 未记录；JsonDiff 执行后剪贴板被"正在打开差异对比..."文案覆盖；`ExecuteRequested` 事件无人订阅（死代码）；`_jsonCrack/_jsonHero.StartAsync()` fire-and-forget 可能吞未观察异常。
- P2：README(win-x64) 与 CI(win-x86) 不一致；DIFFERENCES 字体描述与 XAML(Maple Mono NF CN 优先) 漂移；JsonCrackServer 资源缺失提示仍提 Electron；打开脚本目录走浏览器 file:// 而非文件管理器；VSCode diff 临时文件不清理；窗口标题"文本小军刀"与托盘"字符串小工具"不统一；Avalonia.Svg.Skia 11.3.0 与其他 11.3.2 不对齐；Program.cs 强制软件渲染缺注释；git scheme 的 `.git` 替换是全量替换；根目录空 bin/；RemoteHttp 无大小上限。

用户尚未要求修复，仅要求审查。下一步如需修复可从 P0/P1 开始。
